spring.application.name=tiendita

#1 direccion de la base de datos (se llamara tienda_db)
spring.datasource.url=jdbc:mysql://localhost:3306/tienda_db?serverTimezone=UTC
#2 credenciales de mysql
spring.datasource.username=root
spring.datasource.password=Qwe.123*

# 3. Le decimos a Hibernate que actualice las tablas automáticamente si cambiamos el código Java
spring.jpa.hibernate.ddl-auto=update
# 4. (Opcional) Para ver en consola el código SQL que Hibernate crea por debajo
spring.jpa.show-sql=true